
@{
    string strTitle =
        string.Format("{0} {1}",
        Resources.Action.ListOf,
        Resources.Model.Payment.EntitiesName);

    ViewBag.Title = strTitle;
}

<h2>@strTitle</h2>
@*@using (Html.BeginForm(new { @class = "form-horizontal", role = "form" }))*@