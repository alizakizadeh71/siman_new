@model ViewModels.Areas.Administrator.Request.IndexViewModel
@using Utilities.PersianDate

@{
    string strTitle =
        string.Format("{0} {1}",
        Resources.Action.ListOf,
        Resources.Model.Request.EntitiesName);
}
<style>
    input#CompanyName {
        color: gray;
        padding: 6px 12px;
        border: 1px solid #cccccc;
        border-radius: 4px;
    }

    /* حذف ارتفاع ثابت برای جلوگیری از تداخل با متن‌های طولانی */
    .k-grid td {
        white-space: nowrap; /* پیش فرض nowrap برای همه */
        text-overflow: ellipsis;
        vertical-align: middle !important;
        padding-top: 8px !important;
        padding-bottom: 8px !important;
    }

    /* تنظیمات هدر برای جلوگیری از سه نقطه شدن */
    .k-grid-header .k-header {
        white-space: normal !important;
        text-align: center !important;
        vertical-align: middle !important;
    }

        .k-grid-header .k-header .k-link {
            padding-left: 15px !important;
        }
</style>


<script type="text/javascript">

    function AllowNumbersOnly(e) {
        var code = (e.which) ? e.which : e.keyCode;
        if (code > 31 && (code < 48 || code > 57)) {
            e.preventDefault();
        }
    }

    // باز شدن در صفحه جدید
    $(document).on('click', 'a.btn-blank', function (e) {
        e.preventDefault();
        var url = $(this).attr('href');
        window.open(url, '_blank');
    });

    function Comma(Num) {
        if (!Num) return Num;
        Num = Num.toString().replace(/,/g, '');
        var parts = Num.split('.');
        parts[0] = parts[0].replace(/\B(?=(\d{3})+(?!\d))/g, ",");
        return parts.join('.');
    }

    // تابع دریافت پارامترهای جستجو به صورت داینامیک برای گرید
    function getSearchParameters() {
        return {
            AmountPaid: $("input#AmountPaid").val(),
            InvoiceNumber: $("input#InvoiceNumber").val(),
            BuyerMobile: $("input#BuyerMobile").val(),
            ProductName: $("select#ProductName").val(),
            ProductType: $("select#ProductType").val(),
            PackageType: $("select#PackageType").val(),
            FactoryName: $("select#FactoryName").val(),
            Province: $("select#Province").val(),
            City: $("select#City").val(),
            stringFinalApprove: $("select#stringFinalApprove").val(),
            RequestState: $("select#RequestState").val(),
            Tonnage: $("input#Tonnage").val() || $("select#Tonnage").val(),
            StartDate: $("input#StartDate").val(),
            EndDate: $("input#EndDate").val(),
            PayStartDate: $("input#PayStartDate").val(),
            PayEndDate: $("input#PayEndDate").val(),
            FromAmount: $("#FromAmount").val() ? $("#FromAmount").val().replace(/,/g, '') : "",
            ToAmount: $("#ToAmount").val() ? $("#ToAmount").val().replace(/,/g, '') : ""
        };
    }

    var rowNumber = 0;

    $(document).ready(function () {

        $('#StartDate').datepicker({ dateFormat: 'yy/mm/dd', autoSize: true, showyear: true, changeYear: true });
        $('#EndDate').datepicker({ dateFormat: 'yy/mm/dd', autoSize: true, showyear: true, changeYear: true });
        $('#PayStartDate').datepicker({ dateFormat: 'yy/mm/dd', autoSize: true, showyear: true, changeYear: true });
        $('#PayEndDate').datepicker({ dateFormat: 'yy/mm/dd', autoSize: true, showyear: true, changeYear: true });

        $("#SearchPanelHeader").click(function () {
            $("#SearchPanelBody").toggle(1000);
        });

        var varDataSource =
            new kendo.data.DataSource({
                pageSize: 10,
                serverPaging: true,
                serverSorting: true,
                serverFiltering: true,
                serverAggregates: false,

                transport: {
                    read: {
                        type: "POST",
                        dataType: "json",
                        url: "/Administrator/Request/Search",
                        data: getSearchParameters
                    },
                },

                requestStart: function (e) {
                    $("button#btnSearch").hide();
                },
                requestEnd: function (e) {
                    $("button#btnSearch").show();
                },
                schema: {
                    data: function (e) { return (e.Data); },
                    total: function (e) { return (e.Total); },
                },
            });

        $("div#grid").kendoGrid({
            sortable: { mode: "multiple", allowUnsort: true, },
            pageable: {
                info: true, input: true, refresh: true, numeric: true, buttonCount: 5, previousNext: true, pageSize: [5, 10, 25, 50, 100],
                messages: { morePages: "صفحات بیشتر", page: "شماره صفحه", }
            },
            groupable: true, filterable: true, scrollable: true, reorderable: true,
            columnMenu: { columns: true, filterable: true, messages: { lock: "قفل شده", unlock: "قفل نشده", } },
            navigatable: true, selectable: "multiple row",
            dataSource: varDataSource,
            dataBinding: function () {
                rowNumber = (this.dataSource.page() - 1) * this.dataSource.pageSize();
            },
            columns:
                [
                    {
                        hidden: false, width: "40px", title: "#",
                        attributes: { style: "text-align: center; vertical-align: middle;" },
                        template: function (dataItem) { return ++rowNumber; }
                    },
                    {
                        title: "عملیات", hidden: false, width: "220px",
                        attributes: { style: "text-align: center; vertical-align: middle;" },
                        template: function (dataItem) {
                            var strTemplate = "";

                            strTemplate += '<a data-toggle="tooltip" data-placement="top" title="نمایش" style="margin-left:8px;" href="/Administrator/Request/display/' + dataItem.Id + '">';
                            strTemplate += '<img src="/Content/Icon/display.png" style="width: 20px; height: 20px; vertical-align: middle;" /></a>';

                            strTemplate += '<a data-toggle="tooltip" data-placement="top" title="ویرایش" href="/Administrator/Request/Edit/' + dataItem.Id + '">';
                            strTemplate += '<img src="/Content/Icon/edit.png" style="width: 20px; height: 20px; vertical-align: middle;" /></a>';

                            if (dataItem.RequestState === 3 || dataItem.RequestState === 4) {
                                // مسیر به اکشن متد DriverInfo اشاره دارد
                                var driverInfoUrl = '/Administrator/Request/DriverInfo/' + dataItem.Id;

                                strTemplate += '<a class="btn btn-sm btn-info" data-toggle="tooltip" data-placement="top" title="اطلاعات راننده" href="' + driverInfoUrl + '" style="margin-right:8px; padding: 2px 6px;">';
                                // آیکون کامیون (SVG)
                                strTemplate += '<svg width="16" height="16" fill="currentColor" viewBox="0 0 16 16"><path d="M0 3.5A1.5 1.5 0 0 1 1.5 2h9A1.5 1.5 0 0 1 12 3.5V5h1.02a1.5 1.5 0 0 1 1.17.563l1.481 1.85a1.5 1.5 0 0 1 .329.938V10.5a1.5 1.5 0 0 1-1.5 1.5H14a2 2 0 1 1-4 0H5a2 2 0 1 1-3.998-.085A1.5 1.5 0 0 1 0 10.5v-7zm1.294 7.456A1.999 1.999 0 0 1 4.732 11h5.536a2.01 2.01 0 0 1 .732-.732V3.5a.5.5 0 0 0-.5-.5h-9a.5.5 0 0 0-.5.5v7a.5.5 0 0 0 .294.456zM12 10a2 2 0 0 1 1.732 1h.768a.5.5 0 0 0 .5-.5V8.35a.5.5 0 0 0-.11-.312l-1.48-1.85A.5.5 0 0 0 13.02 6H12v4zm-9 1a1 1 0 1 0 0 2 1 1 0 0 0 0-2zm9 0a1 1 0 1 0 0 2 1 1 0 0 0 0-2z"/></svg></a>';
                            }

                            strTemplate += '&nbsp;&nbsp;&nbsp;';

                            var invoiceNumber = dataItem.InvoiceNumber;

                            if (invoiceNumber === null || invoiceNumber === undefined || invoiceNumber === "" || invoiceNumber === 0 || invoiceNumber === "0") {
                                strTemplate += "<span style='color:red;'>بدون فاکتور</span>";
                            } else {
                                var viewUrl = '/Administrator/Request/ShowFactor?invoicenumber=' + invoiceNumber;
                                var pdfUrl = viewUrl + '&isPdf=true';

                                strTemplate += '<a class="btn btn-sm btn-primary btn-blank" data-toggle="tooltip" data-placement="top" title="نمایش فاکتور" href="' + viewUrl + '" style="margin-left:8px; padding: 2px 6px;">';
                                strTemplate += '<svg width="16" height="16" fill="currentColor" viewBox="0 0 16 16"><path d="M16 8s-3-5.5-8-5.5S0 8 0 8s3 5.5 8 5.5S16 8 16 8z"/><circle cx="8" cy="8" r="3" fill="#0d6efd"></circle></svg></a>';

                                strTemplate += '<a class="btn btn-sm btn-danger btn-blank" data-toggle="tooltip" data-placement="top" title="دانلود PDF" href="' + pdfUrl + '" style="padding: 2px 6px;">';
                                strTemplate += '<svg width="16" height="16" fill="currentColor" viewBox="0 0 16 16"><path d="M4 0h6l4 4v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V2a2 2 0 0 1 2-2z"/><path d="M8 5v6M6 9l2 2 2-2" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></a>';
                            }

                            return strTemplate;
                        }
                    },
                    { field: "StringInsertDateTime", title: "تاریخ", width: "100px" },
                    { field: "InvoiceNumber", title: "فاکتور", width: "100px" },
                    {
                        field: "BuyerName", title: "نام مشتری", width: "130px",
                        template: function (dataItem) {
                            if (dataItem.BuyerName && dataItem.BuyerName.toLowerCase() === "guest") {
                                return "مهمان";
                            }
                            return dataItem.BuyerName ? dataItem.BuyerName : "";
                        }
                    },
                    { field: "BuyerMobile", title: "موبایل", width: "115px" },
                    {
                        field: "AmountPaid", title: "مبلغ پرداختی", width: "140px",
                        template: "#= AmountPaid ? Comma(AmountPaid) : '0' #"
                    },
                    {
                        field: "RequestState", title: "وضعیت", width: "110px",
                        template: function (dataItem) {
                            if (dataItem.RequestState === 1) return "<span class='label label-warning' style='font-size:11px;'>تایید مالی نشده</span>";
                            if (dataItem.RequestState === 2) return "<span class='label label-success' style='font-size:11px;'>تایید مالی شده</span>";
                            if (dataItem.RequestState === 3) return "<span class='label label-info' style='font-size:11px;'>در جریان ارسال</span>";
                            if (dataItem.RequestState === 4) return "<span class='label label-primary' style='font-size:11px;'>تحویل داده شده</span>";
                            return "<span class='label label-default' style='font-size:11px;'>نامشخص</span>";
                        }
                    },
                    { field: "StringTonnage", title: "تناژ", width: "90px" },
                    { field: "StringProductType", title: "تیپ (نوع محصول)", width: "160px" },
                    { field: "StringPackageType", title: "بسته‌بندی", width: "110px" },
                    { field: "StringFactoryName", title: "کارخانه", width: "110px" },
                    {
                        field: "CarrierName", title: "باربری", width: "100px",
                        template: function (dataItem) {
                            if (dataItem.CarrierName === "بدون باربری" || !dataItem.CarrierName) {
                                return "<span class='text-muted'>ثبت نشده</span>";
                            }
                            return "<span style='font-weight:bold; color:#2c3e50;'>" + dataItem.CarrierName + "</span>";
                        }
                    },
                    {
                        field: "DriverName",
                        title: "نام راننده",
                        width: "120px",
                        template: function (dataItem) {
                            // فقط برای وضعیت ۳ و ۴ نمایش داده شود
                            if (dataItem.RequestState === 3 || dataItem.RequestState === 4) {
                                return dataItem.DriverName ? dataItem.DriverName : "<span class='text-muted'>نامشخص</span>";
                            }
                            return "<span class='text-muted'>-</span>";
                        }
                    },
                    {
                        field: "DriverMobile",
                        title: "موبایل راننده",
                        width: "110px",
                        template: function (dataItem) {
                            if (dataItem.RequestState === 3 || dataItem.RequestState === 4) {
                                return dataItem.DriverMobile ? dataItem.DriverMobile : "<span class='text-muted'>نامشخص</span>";
                            }
                            return "<span class='text-muted'>-</span>";
                        }
                    },
                    {
                        field: "DriverLicensePlate",
                        title: "پلاک خودرو",
                        width: "120px",
                        template: function (dataItem) {
                            if (dataItem.RequestState === 3 || dataItem.RequestState === 4) {
                                // اضافه شدن استایل اندازه فونت به پلاک اصلی و پلاک نامشخص
                                return dataItem.DriverLicensePlate
                                    ? "<span style='font-size: 13px;'>" + dataItem.DriverLicensePlate + "</span>"
                                    : "<span class='text-muted' style='font-size: 13px;'>نامشخص</span>";
                            }
                            return "<span class='text-muted' style='font-size: 11px;'>-</span>";
                        }
                    },
                    { field: "MahalTahvil", title: "محل تحویل", width: "120px" },
                    {
                        field: "Address", title: "آدرس", width: "120px",
                        attributes: { style: "white-space: normal !important; word-break: break-word; min-width: 120px;" }
                    },
                    {
                        field: "Description", title: "توضیحات", width: "140px",
                        attributes: { style: "white-space: normal !important; word-break: break-word; min-width: 140px;" }
                    },
                    { field: "StringProductName", title: "محصول", width: "100px" },
                ]
        });

        $("button#btnSearch").click(function () {
            var varGrid = $('div#grid').data("kendoGrid");
            varGrid.dataSource.page(1);
        });
    });
</script>



<section id="Requests" class="form-horizontal" role="form">

    <div class="panel panel-info">
        <div class="panel-heading" style="cursor: pointer;" id="SearchPanelHeader">
            جستجو ( کلیک کنید...!)
        </div>
        <div class="col-md-12 panel-body" id="SearchPanelBody" style="display: none;">
            <div class="col col-md-6">
                <!-- دراپ‌داون وضعیت درخواست به فرم جستجو اضافه شد -->
                <div class="form-group">
                    @Html.LabelFor(model => model.RequestState, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8">
                        @Html.DropDownListFor(model => model.RequestState, null, Resources.Caption.SelectAnItem, new { @class = "form-control" })
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.ProductName, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8">
                        @Html.DropDownListFor(model => model.ProductName, null, Resources.Caption.SelectAnItem, new { @class = "form-control", @onchange = "onChangeProductName()" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.PackageType, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8" id="DivPackageTypeId">
                        @Html.DropDownListFor(model => model.PackageType, null, Resources.Caption.SelectAnItem, new { @class = "form-control", @onchange = "onChangePackageType()" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.Province, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8">
                        @Html.DropDownListFor(model => model.Province, null, Resources.Caption.SelectAnItem, new { @class = "form-control", @onchange = "onChangeProvince()" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.Tonnage, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8" id="DivTonnageId">
                        @Html.TextBoxFor(model => model.Tonnage, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.StartDate, new { @class = "col-md-4 control-label" })
                    <div class="col col-md-8">
                        @Html.TextBoxFor(model => model.StartDate, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.PayStartDate, new { @class = "col-md-4 control-label" })
                    <div class="col col-md-8">
                        @Html.TextBoxFor(model => model.PayStartDate, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.FromAmount, new { @class = "col-md-4 control-label" })
                    <div class="col col-md-8">
                        @Html.TextBoxFor(model => model.FromAmount, new { @class = "form-control", @onkeypress = "return AllowNumbersOnly(event)", @onkeyup = "javascript:this.value=Comma(this.value);" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.BuyerMobile, new { @class = "col-md-4 control-label" })
                    <div class="col col-md-8">
                        @Html.TextBoxFor(model => model.BuyerMobile, new { @class = "form-control", @onkeypress = "return AllowNumbersOnly(event)" })
                    </div>
                </div>
            </div>

            <div class="col col-md-6">
                <!-- برای بالانس شدن ستون‌ها، stringFinalApprove را به بالا منتقل کردم -->
                <div class="form-group">
                    @Html.LabelFor(model => model.stringFinalApprove, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8">
                        @Html.DropDownListFor(model => model.stringFinalApprove, null, Resources.Caption.SelectAnItem, new { @class = "form-control" })
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.ProductType, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8" id="DivProductTypeId">
                        @Html.DropDownListFor(model => model.ProductType, null, Resources.Caption.SelectAnItem, new { @class = "form-control", @onchange = "onChangeProductType()" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.FactoryName, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8" id="DivFactoryNameId">
                        @Html.DropDownListFor(model => model.FactoryName, null, Resources.Caption.SelectAnItem, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.City, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8" id="DivCityId">
                        @Html.DropDownListFor(model => model.City, null, Resources.Caption.SelectAnItem, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.InvoiceNumber, new { @class = "col-md-4 control-label" })
                    <div class="col col-md-8">
                        @Html.TextBoxFor(model => model.InvoiceNumber, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.EndDate, new { @class = "col-md-4 control-label" })
                    <div class="col col-md-8">
                        @Html.TextBoxFor(model => model.EndDate, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.PayEndDate, new { @class = "col-md-4 control-label" })
                    <div class="col col-md-8">
                        @Html.TextBoxFor(model => model.PayEndDate, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.ToAmount, new { @class = "col-md-4 control-label" })
                    <div class="col col-md-8">
                        @Html.TextBoxFor(model => model.ToAmount, new { @class = "form-control", @onkeypress = "return AllowNumbersOnly(event)", @onkeyup = "javascript:this.value=Comma(this.value);" })
                    </div>
                </div>
            </div>

            <div class="form-group" style="clear:both;">
                <div class="col-md-offset-3 col-md-8" style="margin-top:15px; text-align:center;">
                    <button type="button" id="btnSearch" class="btn btn-primary btn-icon btn-green icon-alarmclock">@Resources.OPS.Button.Search</button>
                </div>
            </div>
        </div>
    </div>

    <br />

    <div class="panel panel-primary">
        <div class="panel-heading">
            @strTitle
        </div>
        <div class="col-md-12 panel-body">
            <div class="col-md-12">
                <div class="row">
                    <div style="float:left;width:100%;">
                        <a class="btn btn-default" data-toggle="tooltip" data-placement="top" title="@(Resources.Action.BackToMain)"
                           href="/HomeMain/Main/" style="float: left;width:150px;">
                            <span style="float:right;">@Resources.Action.BackToMain</span><img style="float:left;" src='/Content/Icon/Back.png' />
                        </a>
                    </div>
                </div>
                <br />
                <div class="row">
                    <div data-role="grid" id="grid" class="k-content k-rtl grid k-grid k-widget k-reorderable" style="width:100%;">
                    </div>
                </div>
            </div>
        </div>
        <div class="panel-footer"></div>
    </div>
</section>

<!-- بخش توابع جاوااسکریپت Dropdownهای وابسته‌ (بدون تغییر) -->
<script type="text/javascript">
    function onChangeProvince() {
        $.ajax({
            url: '/Administrator/HeadOfFactor/GetCities', type: 'post', data: { provinceId: $("#Province").val() },
            success: function (data) {
                $("#DivCityId").empty();
                var select = $("<select class='form-control' id='City' name='City' data-rel='chosen'>");
                var option1 = "<option value='@System.Guid.Empty'>" + "از لیست انتخاب نمایید" + "</option>";
                select.append(option1);
                $.each(data, function (i, city) {
                    var option=('<option value="' + city.Id + '"> ' + city.Name + '</option>');
                    select.append(option);
                })
                $("#DivCityId").append(select);
                $('#City>option:eq(1)').prop('selected', true);
            }
        });
    }

    function onChangeProductName() {
        $.ajax({
            url: '/Administrator/Cement/GetProductType', type: 'post', data: { ProductNameId: $("#ProductName").val() },
            success: function (data) {
                $("#DivProductTypeId").empty();
                var select = $("<select class='form-control' onchange = 'onChangeProductType()' id='ProductType' name='ProductType' data-rel='chosen'>");
                var option1 = "<option value='@System.Guid.Empty'>" + "از لیست انتخاب نمایید" + "</option>";
                select.append(option1);
                $.each(data, function (i, ProductType) {
                    var option = ('<option value="' + ProductType.Id + '"> ' + ProductType.Name + '</option>');
                    if (i == 0) { onChangeProductType(ProductType.Id); }
                    select.append(option);
                })
                $("#DivProductTypeId").append(select);
                $('#ProductType>option:eq(1)').prop('selected', true);
            }
        });

        $.ajax({
            url: '/Administrator/Cement/GetFactoryName', type: 'post', data: { ProductNameId: $("#ProductName").val() },
            success: function (data) {
                $("#DivFactoryNameId").empty();
                var select = $("<select class='form-control' id='FactoryName' name='FactoryName' data-rel='chosen'>");
                var option1 = "<option value='@System.Guid.Empty'>" + "از لیست انتخاب نمایید" + "</option>";
                select.append(option1);
                $.each(data, function (i, FactoryName) {
                    var option = ('<option value="' + FactoryName.Id + '"> ' + FactoryName.Name + '</option>');
                    select.append(option);
                })
                $("#DivFactoryNameId").append(select);
                $('#FactoryName>option:eq(1)').prop('selected', true);
            }
        });
    }

    function onChangeProductType(ProductTypee) {
        if (ProductTypee == undefined) { ProductTypee = $("#ProductType").val(); }
        $.ajax({
            url: '/Administrator/Cement/GetPackageType', type: 'post', data: { ProductTypeId: ProductTypee },
            success: function (data) {
                $("#DivPackageTypeId").empty();
                var select = $("<select class='form-control' onchange = 'onChangePackageType()'  id='PackageType' name='PackageType' data-rel='chosen'>");
                var option1 = "<option value='@System.Guid.Empty'>" + "از لیست انتخاب نمایید" + "</option>";
                select.append(option1);
                $.each(data, function (i, PackageType) {
                    var option = ('<option value="' + PackageType.Id + '"> ' + PackageType.Name + '</option>');
                    select.append(option);
                })
                $("#DivPackageTypeId").append(select);
                $('#PackageType>option:eq(1)').prop('selected', true);
                onChangePackageType();
            }
        });
    }

    function onChangePackageType() {
        $.ajax({
            url: '/Administrator/Cement/GetTonnage', type: 'post', data: { PackageTypeId: $("#PackageType").val() },
            success: function (data) {
                $("#DivTonnageId").empty();
                var select = $("<select class='form-control' id='Tonnage' name='Tonnage' data-rel='chosen'>");
                var option1 = "<option value='@System.Guid.Empty'>" + "از لیست انتخاب نمایید" + "</option>";
                select.append(option1);
                $.each(data, function (i, Tonnage) {
                    var option = ('<option value="' + Tonnage.Id + '"> ' + Tonnage.Name + '</option>');
                    select.append(option);
                })
                $("#DivTonnageId").append(select);
                $('#Tonnage>option:eq(1)').prop('selected', true);
            }
        });
    }
</script>
