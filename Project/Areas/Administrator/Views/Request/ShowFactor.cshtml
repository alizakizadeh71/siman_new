@model ViewModels.Areas.Administrator.Cement.CementViewModel

@{
    Layout = "~/Views/Shared/_Layouts/_SiteForPrint.cshtml";
}

<!doctype html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>فاکتور فروش</title>

    <style>
        @@font-face {
            font-family: 'Vazir';
            src: url('/Content/fonts/Vazir.ttf') format('truetype');
            font-weight: normal;
            font-style: normal;
        }

        body {
            font-family: 'Vazir', tahoma, sans-serif !important;
            background: #f8f9fa;
            padding: 20px;
            direction: rtl;
            margin: 0;
            font-size: 14px;
        }

        .invoice {
            background: #fff;
            padding: 24px;
            border-radius: 12px;
            box-shadow: 0 6px 18px rgba(0,0,0,0.06);
            max-width: 900px;
            margin: 0 auto;
        }

        .small-muted {
            color: #6c757d;
            font-size: 0.9rem;
        }

        .signature {
            height: 70px;
            border-bottom: 1px dashed #ddd;
            margin-bottom: 6px;
        }

        .signature-img {
            width: 150px;
            opacity: 0.55;
            mix-blend-mode: multiply;
        }

        /* جدول */
        .table {
            width: 100% !important;
            table-layout: fixed !important;
            border-collapse: collapse !important;
            direction: rtl;
            word-wrap: break-word;
            word-break: break-word;
            white-space: normal;
            font-size: 14px;
        }

            .table th, .table td {
                padding: 6px 8px;
                text-align: center;
                border: 1px solid #dee2e6;
                vertical-align: middle;
                word-wrap: break-word;
                white-space: normal;
            }

            .table th {
                background-color: #f8f9fa;
            }

        .text-end {
            text-align: right !important;
        }

        .text-center {
            text-align: center !important;
        }

        /* تنظیمات container */
        .container {
            max-width: 920px;
            margin: 0 auto;
        }

        /* Flex برای کنار هم قرار گرفتن دو بخش */
        .header-flex,
        .buyer-seller-flex {
            display: flex !important;
            flex-wrap: nowrap !important;
            justify-content: space-between;
            gap: 20px;
            width: 100%;
            box-sizing: border-box;
        }

            /* عرض تقریبی هر بخش 48% */
            .header-flex > div,
            .buyer-seller-flex > div {
                flex: 0 0 48%;
                max-width: 48%;
            }

        /* کنترل اسکرول جدول */
        .table-responsive {
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
        }

        /* تنظیمات مخصوص پرینت */
        @@media print {
            body {
                background: none !important;
                padding: 0 !important;
                margin: 0 !important;
                direction: rtl;
                font-size: 12pt !important;
            }

            .container {
                max-width: 100% !important;
                margin: 0 !important;
                padding: 0 !important;
            }

            .invoice {
                box-shadow: none !important;
                padding: 0 !important;
                border-radius: 0 !important;
                max-width: 100% !important;
            }

            .table-responsive {
                overflow-x: visible !important;
                -webkit-overflow-scrolling: auto !important;
            }

            .table {
                width: 100% !important;
                table-layout: fixed !important;
                border-collapse: collapse !important;
                font-size: 12pt !important;
                white-space: normal !important;
                word-wrap: break-word !important;
                word-break: break-word !important;
            }

                .table th,
                .table td {
                    padding: 6px 8px !important;
                    border: 1px solid #000 !important;
                    word-wrap: break-word !important;
                    white-space: normal !important;
                    text-align: center !important;
                }

            hr {
                display: none;
            }
            /* اجباری کردن flex کنار هم بودن در پرینت */
            .header-flex,
            .buyer-seller-flex {
                display: flex !important;
                flex-wrap: nowrap !important;
                justify-content: space-between !important;
                gap: 20px !important;
                width: 100% !important;
                box-sizing: border-box !important;
            }

                .header-flex > div,
                .buyer-seller-flex > div {
                    flex: 0 0 48% !important;
                    max-width: 48% !important;
                }

            .p-3 {
                padding: 12px !important;
            }

            .text-end {
                text-align: right !important;
            }

            body, html {
                overflow-x: visible !important;
            }

            .no-print {
                display: none !important;
            }
        }
    </style>
</head>

<body>

    @{
        // استخراج مقدار تناژ عددی از رشته
        double tonnageValue = 0;
        if (!string.IsNullOrWhiteSpace(Model.StringTonnage))
        {
            var digitsOnly = new string(Model.StringTonnage.Where(char.IsDigit).ToArray());
            if (!string.IsNullOrWhiteSpace(digitsOnly))
            {
                tonnageValue = Convert.ToDouble(digitsOnly);
            }
        }

        double amountWithoutTax = Model.AmountPaid / 1.1;
        double taxAmount = amountWithoutTax * 0.1;

        string unitPrice = tonnageValue > 0 ? (Model.AmountPaid / tonnageValue).ToString("N0") : "0";
    }

    <div class="container">
        <div class="invoice">

            <!-- header -->
            <div class="header-flex mb-3">
                <div class="company-info border rounded p-3">
                    <h5 class="mb-0">مصالح پخش برخط</h5>
                    <div class="small-muted">شناسه ملی : 14012802684</div>
                    <div class="small-muted">آدرس: کرمان، کوهبنان</div>
                    <div class="small-muted">کد پستی : 7784165165</div>
                    <div class="small-muted">شماره ثبت : 1189 </div>
                </div>

                <div class="invoice-info border rounded p-3 text-end">
                    <div><strong>فاکتور فروش کالا و خدمات</strong></div>
                    <div class="small-muted">سریال: <strong>@Model.InvoiceNumber</strong></div>
                    <div class="small-muted">تاریخ: <strong>@Model.StringInsertDateTime</strong></div>
                </div>
            </div>

            <hr />

            <!-- buyer + seller -->
            <div class="buyer-seller-flex mb-3">
                <div class="buyer-info p-3 border rounded h-100">
                    <h6 class="mb-2">مشخصات خریدار</h6>
                    <div>نام: <strong>@Model.BuyerName</strong></div>
                    <div class="small-muted">آدرس: @Model.Address</div>
                    <div class="small-muted">شماره همراه: @Model.BuyerMobile</div>
                    <div class="small-muted">کد ملی: @Model.BuyerNationalCode</div>
                </div>

                <div class="seller-info p-3 border rounded h-100">
                    <h6 class="mb-2">مشخصات فروشنده</h6>
                    <div>مصالح پخش برخط</div>
                    <div class="small-muted">آدرس: کرمان ، کوهبنان</div>
                    <div class="small-muted">کد پستی: 7784165165</div>
                    <div class="small-muted">تلفن مسئول فروش: 09051510102</div>
                </div>
            </div>

            <!-- items table -->
            <div class="table-responsive">
                <table class="table table-bordered align-middle">
                    <thead>
                        <tr class="text-center">
                            <th style="width:6%;">ردیف</th>
                            <th style="width:44%;">شرح کالا یا خدمات</th>
                            <th style="width:12%;">مقدار</th>
                            <th style="width:18%;">مبلغ واحد</th>
                            <th style="width:18%;">مبلغ کل</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td class="text-center">1</td>
                            <td style="word-break: break-word; white-space: normal;">
                                @string.Join(" ", new[] {
                                    Model.StringProductName,
                                    Model.StringProductType,
                                    Model.StringPackageType,
                                    Model.StringFactoryName
                                })
                            </td>
                            <td class="text-center">@Model.StringTonnage</td>
                            <td class="text-end">@unitPrice</td>
                            <td class="text-end">@Model.AmountPaid.ToString("N0")</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <!-- totals -->
            <div class="row justify-content-end mt-3">
                <div class="col-md-6">
                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <td class="small-muted">جمع مبلغ کل (بدون مالیات)</td>
                                <td class="text-end"><strong>@amountWithoutTax.ToString("N0")</strong> ریال</td>
                            </tr>
                            <tr>
                                <td class="small-muted">جمع مالیات و عوارض (۱۰٪)</td>
                                <td class="text-end"><strong>@taxAmount.ToString("N0")</strong> ریال</td>
                            </tr>
                            <tr>
                                <td class="small-muted">مبلغ تخفیف</td>
                                <td class="text-end">0</td>
                            </tr>
                            <tr class="border-top">
                                <td class="fw-bold">جمع کل</td>
                                <td class="text-end fw-bold">@Model.AmountPaid.ToString("N0") ریال</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- signatures -->
            <div class="row mt-3">
                <div class="col-md-8">
                    <h6>توضیحات</h6>
                    <p class="small-muted">@Model.Description</p>
                </div>
                <div class="col-md-4 text-center">
                    <div class="mb-3">
                        <div class="signature">
                            <img src="/Content/Images/signature-stamp.jpg" class="signature-img" />
                        </div>
                        <div class="small-muted">مهر و امضا فروشنده</div>
                    </div>
                    <div>
                        <div class="signature"></div>
                        <div class="small-muted">مهر و امضا خریدار</div>
                    </div>
                </div>
            </div>

            <hr />

            <div class="d-flex justify-content-between small-muted">
                <div>شماره ثبت: 10840047614</div>
                <div>تلفن تماس: 03532429082</div>
            </div>

        </div>
    </div>

    <!-- بخش تایید مالی (فقط در حالت نمایشگر مرورگر دیده می‌شود، در پرینت مخفی است) -->
    @if (Model.RequestState == 1)
    {
        <div class="container no-print" style="margin-top: 20px; margin-bottom: 40px;">
            <div class="invoice" style="text-align: right; background: #eef2f3; padding: 20px;">
                <h5 style="color: #2c3e50; margin-bottom: 15px; border-bottom: 2px solid #ccc; padding-bottom: 10px;">بخش تایید مالی سفارش</h5>
                <form action="/Administrator/Request/ApproveFinancial" method="post">
                    <input type="hidden" name="id" value="@Model.Id" />

                    <div style="margin-bottom: 15px;">
                        <label for="description" style="display: block; margin-bottom: 8px; font-weight: bold; color: #333;">توضیحات (اختیاری):</label>
                        <textarea name="description" id="description" rows="3" style="width: 100%; max-width: 100%; padding: 10px; border: 1px solid #ccc; border-radius: 6px; font-family: 'Vazir', tahoma;" placeholder="اگر توضیحاتی برای این تایید مالی دارید، اینجا وارد کنید..."></textarea>
                    </div>

                    <button type="submit" onclick="return confirm('آیا از تایید مالی این فاکتور اطمینان دارید؟');" style="background-color: #28a745; color: white; padding: 10px 20px; border: none; border-radius: 6px; cursor: pointer; font-size: 15px; font-family: 'Vazir', tahoma; font-weight: bold;">
                        ✓ تایید مالی و ارسال به کارتابل باربری
                    </button>
                </form>
            </div>
        </div>
    }

</body>
</html>
